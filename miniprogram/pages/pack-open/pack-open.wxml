<view class="page pack-page">
  <view wx:if="{{!revealed}}" class="pack-stage panel {{activeTheme.packClass}} {{openingClass}}">
    <view class="burst"></view>
    <view class="spark spark-a"></view>
    <view class="spark spark-b"></view>
    <view class="spark spark-c"></view>
    <view class="pack-bag">
      <view class="pack-mark">{{packMark}}</view>
    </view>
    <view class="pack-title">{{packTitle}}</view>
    <view class="muted">{{packHint}}</view>
    <button class="primary-btn open-btn" disabled="{{opening}}" bindtap="beginOpen">{{openButtonText}}</button>
  </view>

  <view wx:else>
    <view class="result-title reveal-title">获得 {{pack.length}} 张卡</view>
    <view class="result-subtitle">新卡 {{newCount}} 张</view>
    <view class="card-grid">
      <view wx:for="{{pack}}" wx:key="id" class="reveal-card rarity-{{item.rarity}}" style="animation-delay: {{index * 120}}ms;">
        <view class="rare-flash"></view>
        <view wx:if="{{item.isNew}}" class="new-badge">NEW</view>
        <card-face card="{{item}}" size="medium" />
      </view>
    </view>
    <button class="secondary-btn open-btn" bindtap="goAlbum">放进收藏本</button>
  </view>
</view>
